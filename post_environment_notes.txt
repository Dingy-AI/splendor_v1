1. Environment validation

Run a few thousand random games and collect statistics such as:

Average game length
Maximum game length
Average score of winner
Distribution of action types
Average number of legal actions

This will catch odd behavior like:

games consistently taking 600 turns,
players almost never buying reserved cards,
nobles never being claimed.
2. Performance profiling

Benchmark the core operations.

For example:

clone()
step()
legal_actions()
action_mask()
observation()

Time each one over 100,000 iterations.

You'll quickly discover where your bottleneck is.

3. Verify clone independence

This is probably the most important test before MCTS.

clone = env.clone()

clone.step(action)

assert env.state != clone.state

Also verify nested objects:

cards
players
bank
visible cards
reserved cards

No shared references.

4. Decide on your MCTS interface

I'd define it now.

Something like

root = Node(state)

while budget:
    leaf = select(root)
    child = expand(leaf)
    value = evaluate(child)
    backup(child, value)

return best_action(root)

Your environment already supports this nicely.

5. Build a random MCTS first

Don't involve a neural network yet.

Use

value = random rollout reward

This lets you verify

expansion
backup
UCB
tree reuse

without wondering whether the network is wrong.

6. Neural network

Only after random MCTS is stable.

Start with something extremely small.

Input:

354 features

Output:

1139-policy logits
1 value

Simple MLP:

354
 ↓
512
 ↓
512
 ↓
policy head
value head

You can always replace it with a Transformer later.

One thing I'd strongly recommend

Before writing MCTS, build a simple heuristic agent.

For example:

Buy if possible.
Otherwise reserve the highest-point affordable-looking card.
Otherwise take gems toward the cheapest visible card.

This takes maybe an hour to write and gives you:

a stronger baseline than random,
something to play against,
a debugging tool for MCTS,
a way to generate initial self-play data if needed.

When your MCTS starts outperforming this heuristic agent, you'll know it's actually learning something instead of merely beating random play.

At this stage, my roadmap would be:

Performance profiling
Heuristic agent
Random-rollout MCTS
Neural network
AlphaZero-style self-play training

That's a natural progression and keeps each new component isolated and easier to debug.

###

Random rollout MCTS
Verify it beats random player
Add heuristics
Replace rollout with your neural network


How I think you should implement it

I would build it in these milestones:

Phase 1
Node

Just stores data.

Phase 2
Random rollout

Given a state

↓

Play randomly

↓

Return winner

No tree yet.

Phase 3
Expansion

Given

Node

create one child.

Phase 4
Selection

Implement UCB.

Phase 5
Backup

Update visits and values.

Phase 6

Combine everything into

search()
# Bug in agent_b mcts 
Evaluation Results
------------------
agent_a_wins: 19
agent_b_wins: 1
ties: 0
deadlocks: 0
games_played: 20
total_steps: 3235
completed_games: 20
agent_a_win_rate: 0.95
agent_b_win_rate: 0.05
average_steps: 161.75

# fixed bug in random roll out ->
10 games 10 simulations 
Evaluation Results
------------------
agent_a_wins: 3
agent_b_wins: 7
ties: 0
deadlocks: 0
games_played: 10
total_steps: 1142
completed_games: 10
agent_a_win_rate: 0.3
agent_b_win_rate: 0.7
average_steps: 114.2

# longer test ->
20 games 20 simulations random vs random rollout
Evaluation Results
------------------
agent_a_wins: 3
agent_b_wins: 16
ties: 0
deadlocks: 1
games_played: 20
total_steps: 2158
completed_games: 19
agent_a_win_rate: 0.15789473684210525
agent_b_win_rate: 0.8421052631578947
average_steps: 107.9


# 20 games 20 simulations random vs heuristic
Evaluation Results
------------------
agent_a_wins: 6
agent_b_wins: 14
ties: 0
deadlocks: 0
games_played: 20
total_steps: 2237
completed_games: 20
agent_a_win_rate: 0.3
agent_b_win_rate: 0.7
average_steps: 111.85


# 20 games 20 simulations random vs heuristic 2
Evaluation Results
------------------
agent_a_wins: 5
agent_b_wins: 14
ties: 0
deadlocks: 1
games_played: 20
total_steps: 2262
completed_games: 19
agent_a_win_rate: 0.2631578947368421
agent_b_win_rate: 0.7368421052631579
average_steps: 113.1

Network model
→ forward-pass tests # DONE
→ policy masking #DONE
→ value/policy losses #DONE
→ replay buffer #DONE
→ one supervised training test #DONE
→ neural evaluation inside MCTS #DONE
→ PUCT #DONE
→ check if expand still chooses an untried action randomly #DONE 

→ self-play #DONE
→ full training loop # DONE 

       1. Generate self-play games
       2. Store (observation, target_policy, target_value)
       3. Sample batches from replay buffer
       4. Run model forward pass
       5. Compute policy + value loss
       6. Backpropagate
       7. Update network weights
       8. Repeat

Self-play action sampling from root visits #DONE 
Test self-play action sampling #DONE
Run a small end-to-end training experiment #DONE
Add basic training logging #DONE

Diagnosing issue with self play # DONE MAYBE?
Fixing small issue in game evaluation #DONE

BUILD agent wrapper for Neural PUCT MODEL #DONE
Build Trained-model evaluation #DONE

compare against a random baseline #DONE 
Save/load checkpoints #DONE

NEED TO TUNE #DONE
Tested different simulations amounts and bug is still here #DONE

Look into issue/bug that is causing take into discard #DONE
  seems that the issue is that the simulations were too small 
  and that its not learning enough due to small number of games

PUCT vs random-rollout MCTS #SKIPPING THIS for now -> I think need to come back
  once I have an actually trained agent.

Chang deep Cloning optimization # DONE 
  334 sec -> 275 sec over two games
Performance profiling/optimization #ON-GOING
  I looked up a dude and he was able to train 25,000 games in 12 hours

increased map_payment_to_card speed # DONE
slow_map_payment_to_card: 2.08 µs/call, 480917 calls/sec 
map_payment_to_card: 0.73 µs/call, 1367806 calls/sec 
Speedup: 2.84x Time reduction: 64.8%

increased speed further see time_reduction_goals.txt #DONE 

create speed reduction with generate_valid_payments to filter out bad cards

#START here tomorrow

1. Cache _encode_card # DONE
2. Re-profile #DONE 
3. Optimize _can_afford_card / duplicate cost calculations #DONE 
4. Re-profile #DONE
get color mapping optimization #done
encode single player #done

_generate_valid_payments() — 6.85s #DONE 
_legal_take_gems() — 1.34s #DONE 
_encode_single_player() — 1.19s #SKIP 
map_payment_to_canonical() — 0.86s #SKIP 
_encode_nobles() — 0.68s #SKIP 

we are going to investigate expand_all_with_priors now #DONE 
the next big 'win' is in mcts 
#we are now at the point where it is less than 3 sec per game with 20 simulations

5. Only then consider deeper architectural changes like tree reuse #DONE 

6. Investigate lazy MCTS child-state creation #DONE


run 800 sims or something like it as a psuedo random rollout
to confirm it is 'working'

add_dirichlet_noise xx # DONE
did a lot of checking we found that prior is creating issues with 
    learning. its "intuition" is not 'good' but I am not sure 
    if it is just the limited state space 

# increased c_puct to 3.0 from 1.5 #DONE 

# need to do a test regarding same seeds and train to see #DONE ISH 
    if the ai is indeed learning 
    if ai is learning here then the algorithm is fine 
    the issue is that the state space is just so damn big 

# THE AI will learn some of the time with V = 0 and uniform P 
# the teacher mode helps it learn







Add persistent replay storage (SQLite) #PKL TIME INSTEAD DONE
Track strength across training # added matlibplot but its not working 

# MAY need to upgrade network into what the ludometer has #ONGOING
# I think we need to do two things 
    #update our neural net into a residual net
    # do a train/data ratio #DONE
        training_steps = round(
           positions_added * 1.5 / batch_size
        )
    # set batch size to 256 

Run a fresh normal AlphaZero experiment. This is the big test:
    fresh residual network
    fresh optimizer
    empty replay
    teacher_mode=False
    200 simulations
    tree reuse ON
    batch 256
    replay ratio 1.5

if it struggles we go back to uniform prior and v = 0
    maybe do sims = 400 or 200?

# later if the above works 
    structured attention + factorized policy
#potential learning rate changes 

https://github.com/RemiFabre/ludometer/blob/main/docs/METHODOLOGY.md#8-three-runs-three-ideas


Tune training/MCTS hyperparameters # NOPE XD 
Add advanced AlphaZero features # HOLD OFF XD 

Current
2-layer MLP
flat 1139 policy

        ↓

Experiment A
Residual MLP + LayerNorm
flat 1139 policy

        ↓

Experiment B
Entity/token representation
attention
structured/factorized policy

# we need to look for ways to do MCTS with variable starting states 


The end goal might be:

8 games
    ↓
each produces one pending leaf
    ↓
batch size 8
    ↓
one model forward
    ↓
8 MCTS searches resume



SQLITE 

WEBSITE-UI -> PLAY TEST


                    ENVIRONMENT
                        │
                        ▼
                  action_mask
                 "what is legal?"
                        │
                        ▼
STATE ───────► NEURAL NETWORK
                        │
                        ▼
                  policy_logits
                        │
                 mask + softmax
                        │
                        ▼
                  policy_probs
            "what does NN prefer?"
                        │
                        ▼
                  PUCT priors
                        │
                        ▼
                      MCTS
                        │
                 search happens
                        │
                        ▼
                 visit counts
                        │
                        ▼
              root_visit_policy()
                        │
                        ▼
                 target_policy
              "what MCTS prefers"
                        │
                        ▼
                   POLICY LOSS
                        │
                        ▼
                 NEURAL NETWORK

run_training(
        env=env,
        model=model,
        optimizer=optimizer,
        replay_buffer=replay_buffer,
        num_iterations=3,
        self_play_games_per_iteration=2,
        simulations=5,
        batch_size=32,
        training_steps=10,
)

ITERATION 1
    ↓
Play 2 self-play games
    ↓
Store all positions in replay buffer
    ↓
Sample 32 positions
Train
    ↓
Sample 32 positions
Train
    ↓
... × 10 training steps

ITERATION 2
    ↓
Play 2 games using UPDATED network
    ↓
Add positions to existing replay buffer
    ↓
10 more training steps

ITERATION 3
    ↓
Play 2 games using UPDATED network
    ↓
Add positions
    ↓
10 more training steps

simulations=5 controls how much thinking MCTS does before making one move:
Game position
   ↓
5 PUCT simulations
   ↓
visit distribution
   ↓
choose move

Think of one simulation as:

Current Splendor position
        ↓
      SELECT
PUCT chooses a promising path
        ↓
      EXPAND
Reach an unexplored position
        ↓
     EVALUATE
Neural network estimates its value
        ↓
      BACKUP
Update visit counts + values

5 simulations
→ very cheap
→ shallow/limited search

50 simulations
→ substantially more search
→ more informed visit distribution

200 simulations
→ much stronger search
→ much more expensive

800 simulations
→ very substantial search
→ potentially extremely expensive

weak initial value network
        ↓
Q differences are tiny/noisy
        ↓
PUCT is heavily influenced by policy prior
        ↓
some early TAKE preference emerges
        ↓
MCTS target often closely mirrors that preference
        ↓
policy learns stronger TAKE preference
        ↓
TAKE at 10 gems creates forced discard chains
        ↓
very long/correlated trajectories
        ↓
many overflow/discard states enter replay
        ↓
training distribution gets stranger

                    PUCT
                      │
          ┌───────────┴───────────┐
          │                       │
     EXPLOITATION              EXPLORATION
          │                       │
          Q                Policy prior (P)
          │                + visit counts
          │                       │
 "What has worked?"       "What should I try?"